# Copy to .env next to docker-compose.yml on the host. NEVER commit the real file.
# Behemoth reads Binance API keys + other secrets from SSM (eu-west-1) using these AWS creds.

AWS_ACCESS_KEY_ID=AKIA...replace-me
AWS_SECRET_ACCESS_KEY=replace-me
